Its a simple python script i made to convert 300 images

just run `python conversor path` to convert all files in this path

it will clone your directory tree structure in a directory named `converted`
